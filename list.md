策略项,需求描述,OpenLDAP ppolicy 支持情况,对应属性 / 实现方式,备注
密码长度范围 - 最小长度,要求密码必须满足最小长度限制,✅ 支持,pwdMinLength,直接配置最小长度数值
密码长度范围 - 最大长度,要求密码必须满足最大长度限制,⚠️ 需扩展,需配合 pw-quality/ 自定义 pqchecker 模块,标准 ppolicy 不提供最大长度限制，需依赖外部校验模块实现
允许输入字符集（小写 / 大写 / 数字 / 非字母数字）,要求密码必须包含指定类型字符（如大小写 + 数字 + 特殊字符）,⚠️ 需扩展,需配合 pw-quality/ 自定义 pqchecker 模块,标准 ppolicy 仅限制长度，不直接限制字符集类型
必须同时包含字符（允许只包含 1 类）,要求密码复杂度需满足多字符类型组合规则,⚠️ 需扩展,需配合 pw-quality/ 自定义 pqchecker 模块,标准 ppolicy 仅控制密码质量检查级别（0 = 关 / 1 = 宽松 / 2 = 强制），不提供具体组合规则
禁止密码包含用户名,要求密码不得与用户名存在包含关系,⚠️ 需扩展,需自定义密码检查脚本 / 模块,原生 ppolicy 不支持该校验逻辑，需二次开发实现
不允许包含特定字符串,要求密码不得使用黑名单字符串（如弱口令、常见序列）,⚠️ 需扩展,需配合 pw-quality/cracklib/ 自定义 checker 模块,标准策略无法直接配置黑名单，需依赖外部模块定义禁用字符串
密码有效期（90 天）,要求密码最长存活 90 天，到期后失效,✅ 支持,pwdMaxAge,配置为 7776000 秒（90 天）
密码最短使用期限（0 天）,要求密码修改后需等待指定时间才能再次修改,✅ 支持,pwdMinAge,配置为 0 秒表示可立即修改
即将到期提醒（到期前 15 天）,要求密码到期前 15 天开始提醒用户,✅ 支持,pwdExpireWarning,配置为 1296000 秒（15 天）
历史密码检查（禁止使用之前 1 次）,要求禁止重复使用最近 1 次的历史密码,✅ 支持,pwdInHistory,配置为 1 表示不能与最近一次密码相同
强制修改密码标记,要求用户下次登录时必须修改密码,✅ 支持,pwdMustChange,配置为 TRUE 时触发强制修改
